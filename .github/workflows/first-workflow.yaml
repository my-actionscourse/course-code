name: my first workflow
on: [push]

jobs:
  run-shell-commands:
    runs-on: ubuntu-latest
    steps:
      - name: Echo a string
        run: echo "Hello Sudhamoy, welcome your first github action project"
      - name: Multiline command
        run: |
          node -v
          npm -v
