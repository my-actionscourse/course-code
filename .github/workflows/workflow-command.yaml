name: workflow commands
on: [push]

jobs:
  testing-wf-commands:
    runs-on: ubuntu-latest
    steps:
      - name: Setting an error message
        run: echo "::error::missing semicolon"
      - name: masking
        run: "::add-mask::Secrete String"
      - name: Grouping
        run: |
          echo "::group::My group Id
          echo "Inside group"
          echo "::endgroup::"
